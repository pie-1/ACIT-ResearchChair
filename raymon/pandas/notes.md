###Data frames:

Key concept
Data frame = A tabular data structure with rows and columns. (2 Dimensional) similar to an excel spreadsheet

##JSON files:
JSON  is the standard format the internet uses to move data around. It organizes information into simple key-value pairs, much like a dictionary.
